# Persistent Node server (`next start`). Do not deploy this app serverless.
JWT_SECRET=change-me-to-a-long-random-string-at-least-32-chars

# Set to 1 to include the simulated OTP in JSON (always on in development).
# OTP_ECHO=1
