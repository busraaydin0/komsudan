---
description: Çok kategorili genişleme — Faz 6 ödeme, sonra 6.5 kategori. Atlama.
alwaysApply: true
---

# Genişleme sırası (Faz 6 → 9)

Kaynak: masaüstü `KOMSUDAN-ROADMAP-GENISLEME.md`. Faz atlama.

- **Tip A delivery:** çamaşır, sonra ev yemeği. Evde hazırla, kapı/nokta teslim.
- **Tip B home_visit:** tamir, klima vb. Eve git, randevu. Faz 8’e kadar doldurma.

## Sıra

6 Ödeme (bitti) → **6.5 kategori soyutlaması (bitti)** → **7 ev yemeği (Tip A)** → 8 home-visit (Tip B) → 9+ review/dispute/log.

Faz 6.5: `camasir` seed, `category_id` backfill, delivery = mevcut çamaşır, `home_visit` iskelet (`CATEGORY_NOT_READY`). Yeni kategori/UI yok.

PWA’yı kırma. Tutar/status client’tan alma. Route’ta SQL yok.
